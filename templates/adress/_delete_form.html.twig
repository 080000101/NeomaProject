<form method="post" action="{{ path('adress_delete', {'id': adress.id}) }}" onsubmit="return confirm('Are you sure you want to delete this item?');">
    <input type="hidden" name="_token" value="{{ csrf_token('delete' ~ adress.id) }}">
    <button class="delete-btn"><img class="delete-logo" src="{{ asset('build/images/delete-logo.png') }}" alt="ACME delete logo"></button>
</form>

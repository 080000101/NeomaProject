{% extends 'base.html.twig' %}

{% block title %}Edit Adress{% endblock %}

{% block body %}
    <div class="edit-background">

        <div class="edit-container">

            <h1 class="edit-title">Modifier une adresse</h1>

            <div class="edit-form">
                {{ include('adress/_form.html.twig', {'button_label': 'Modifier'}) }}
            </div>

        </div>

        {{ include('adress/_delete_form.html.twig') }}

        <a class="return-btn"href="{{ path('contact_index') }}"><img class="retour" src="{{ asset('build/images/retour.png') }}" alt="ACME retour"></a> 

    </div>
{% endblock %}
